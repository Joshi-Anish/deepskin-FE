# API base URL for the DeepSkin backend.
# In dev, leave as /api and the Vite dev server proxies /api to the Django
# backend (see vite.config.js). For a direct connection, point this at the
# backend origin, e.g. http://localhost:8000
VITE_API_BASE_URL=/api
